version: "3.9"

services:
  mosquitto:
    image: eclipse-mosquitto:2
    container_name: entrance-service-mosquitto
    volumes:
      - ./mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf
    restart: unless-stopped

  entrance-service:
    build: .
    container_name: entrance
    depends_on:
      - mosquitto
    environment:
      BROKER: mosquitto
      PORT: 1883
    restart: unless-stopped
